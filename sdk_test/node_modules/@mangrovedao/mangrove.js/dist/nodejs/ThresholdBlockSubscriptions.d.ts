declare namespace ThresholdBlockSubscriptions {
    type blockSubscription = {
        seenCount: number;
        cbs: Set<(n: number) => void>;
    };
}
declare class ThresholdBlockSubscriptions {
    #private;
    constructor(lastSeen: number, seenThreshold: number);
    increaseCount(n: number): void;
    subscribe<T>(n: number, cb: (number: any) => T): Promise<T>;
}
export default ThresholdBlockSubscriptions;
//# sourceMappingURL=ThresholdBlockSubscriptions.d.ts.map